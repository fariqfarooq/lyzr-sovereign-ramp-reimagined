# Enterprise ecosystem logos

The `aws.svg`, `kpmg.svg`, `nvidia.svg`, `hitachi-provided.svg`, `movate.svg`, and `wtw-provided.svg` files came from the supplied take-home assets.

They are intentionally rendered in a neutral enterprise-ecosystem strip instead of being labelled as customer case studies. Customer proof is handled separately below the logo rail.
